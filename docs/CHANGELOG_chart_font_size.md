# Changelog - History Chart Font Size Update

## Changes
- Increased the font size of the **X-axis** ticks on the "ประวัติเคลื่อนไหว" (Movement History) chart from **13px** to **20px** (~1.5x bigger).
- Increased the font size of the **Y-axis** ticks on the same chart to **18px** (from default, ~1.5x bigger).
- Updated the section headers for "ประวัติเคลื่อนไหว" and "รายการเคลื่อนไหว" in the product modal from **0.8rem** to **1.2rem** (1.5x bigger) to maintain visual hierarchy and readability.

## Files Modified
- `app.js`: Updated Chart.js configuration for `archivedHistoryChart`.
- `index.html`: Updated inline styles for modal section headers.

## Context
The user requested to make the axes text 1.5 times bigger for better readability on the movement history chart.
